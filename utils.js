import fs from 'fs'
import filenamifyUrl from 'filenamify-url'
import path from 'path'

export function urlToFilename (err, url){
    return (
        if(err){
            
            cb(err)
        }
        else {
            return filenamifyUrl(url)
        }
    )
}